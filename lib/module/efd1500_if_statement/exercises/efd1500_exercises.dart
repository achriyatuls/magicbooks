// 📝 LEMBAR KERJA SISWA - EFD1500 IF STATEMENT
// Isi fungsi-fungsi berikut sesuai instruksi. Jangan ubah signature.

class Efd1500Exercises {
  // 1. Genap/Ganjil
  static String? exercise1Solution(int number) {
    String? result;
    // 👇 TULIS KODE DI SINI:

    return result;
  }

  // 2. Positif/Negatif/Nol
  static String? exercise2Solution(int num) {
    String? result;
    // 👇 TULIS KODE DI SINI:

    return result;
  }

  // 3. Bandingkan dua angka
  static String? exercise3Solution(int num1, int num2) {
    String? result;
    // 👇 TULIS KODE DI SINI:

    return result;
  }

  // 4-6. Kategori nilai
  static String? exercise4Solution(int score) {
    String? result;
    // 👇 TULIS KODE DI SINI:

    return result;
  }

  static String? exercise5Solution(int score) {
    String? result;
    // 👇 TULIS KODE DI SINI:

    return result;
  }

  static String? exercise6Solution(int score) {
    String? result;
    // 👇 TULIS KODE DI SINI:

    return result;
  }

  // 7. Bilangan Bulat atau Tidak (cek tipe)
  static String? exercise7Solution(num value) {
    String? result;
    // 👇 TULIS KODE DI SINI:
    // Jika value adalah int → "Bilangan Bulat", selainnya → "Bukan Bilangan Bulat"

    return result;
  }

  // 8. Negatif/Positif/Nol
  static String? exercise8Solution(int number) {
    String? result;
    // 👇 TULIS KODE DI SINI:

    return result;
  }

  // 9. number1 > number2 ?
  static bool? exercise9Solution(int number1, int number2) {
    bool? result;
    // 👇 TULIS KODE DI SINI:

    return result;
  }

  // 10. number1 < number2 ?
  static bool? exercise10Solution(int number1, int number2) {
    bool? result;
    // 👇 TULIS KODE DI SINI:

    return result;
  }

  // 11. Apakah Minggu?
  static bool? exercise11Solution(DateTime now) {
    bool? isSunday;
    // 👇 TULIS KODE DI SINI:

    return isSunday;
  }

  // 12. Hitung usia (tahun)
  static int? exercise12Solution(DateTime birthday, DateTime today) {
    int? age;
    // 👇 TULIS KODE DI SINI:

    return age;
  }

  // 13-20. Pengecekan tanggal/hari
  static bool? exercise13Solution(DateTime date) {
    bool? isJanuaryFirst;
    // 👇 TULIS KODE DI SINI:

    return isJanuaryFirst;
  }

  static bool? exercise14Solution(DateTime date) {
    bool? isSunday;
    // 👇 TULIS KODE DI SINI:

    return isSunday;
  }

  static bool? exercise15Solution(DateTime date) {
    bool? isSaturday;
    // 👇 TULIS KODE DI SINI:

    return isSaturday;
  }

  static bool? exercise16Solution(DateTime date) {
    bool? isFriday;
    // 👇 TULIS KODE DI SINI:

    return isFriday;
  }

  static bool? exercise17Solution(DateTime date) {
    bool? isThursday;
    // 👇 TULIS KODE DI SINI:

    return isThursday;
  }

  static bool? exercise18Solution(DateTime date) {
    bool? isWednesday;
    // 👇 TULIS KODE DI SINI:

    return isWednesday;
  }

  static bool? exercise19Solution(DateTime date) {
    bool? isTuesday;
    // 👇 TULIS KODE DI SINI:

    return isTuesday;
  }

  static bool? exercise20Solution(DateTime date) {
    bool? isMonday;
    // 👇 TULIS KODE DI SINI:

    return isMonday;
  }

  // 21. List mengandung angka 3 ?
  static bool? exercise21Solution(List<int> numbers) {
    bool? hasThree;
    // 👇 TULIS KODE DI SINI:

    return hasThree;
  }

  // 22. Semua angka genap ?
  static bool? exercise22Solution(List<int> numbers) {
    bool? allEven;
    // 👇 TULIS KODE DI SINI:

    return allEven;
  }

  // 23. Ada angka > 5 ?
  static bool? exercise23Solution(List<int> numbers) {
    bool? hasGreaterThanFive;
    // 👇 TULIS KODE DI SINI:

    return hasGreaterThanFive;
  }

  // 24. Ada angka < 0 ?
  static bool? exercise24Solution(List<int> numbers) {
    bool? hasLessThanZero;
    // 👇 TULIS KODE DI SINI:

    return hasLessThanZero;
  }

  // 25. Jumlah ganjil == jumlah genap ?
  static bool? exercise25Solution(List<int> numbers) {
    bool? oddCountEqualEvenCount;
    // 👇 TULIS KODE DI SINI:

    return oddCountEqualEvenCount;
  }

  // 26. Nilai terbesar == 5 ?
  static bool? exercise26Solution(List<int> numbers) {
    bool? largestIsFive;
    // 👇 TULIS KODE DI SINI:

    return largestIsFive;
  }

  // 27. Nilai terkecil == 1 ?
  static bool? exercise27Solution(List<int> numbers) {
    bool? smallestIsOne;
    // 👇 TULIS KODE DI SINI:

    return smallestIsOne;
  }

  // 28. Jumlah item == 5 ?
  static bool? exercise28Solution(List<int> numbers) {
    bool? countIsFive;
    // 👇 TULIS KODE DI SINI:

    return countIsFive;
  }

  // 29. Ada duplikat ?
  static bool? exercise29Solution(List<int> numbers) {
    bool? hasDuplicate;
    // 👇 TULIS KODE DI SINI:

    return hasDuplicate;
  }

  // 30. Negatif/Positif/Nol
  static String? exercise30Solution(int number) {
    String? result;
    // 👇 TULIS KODE DI SINI:

    return result;
  }

  // 31. num1 > 0 && num2 > 5
  static bool? exercise31Solution(int num1, int num2) {
    bool? result;
    // 👇 TULIS KODE DI SINI:

    return result;
  }

  // 32. num1 > 0 || num2 > 15
  static bool? exercise32Solution(int num1, int num2) {
    bool? result;
    // 👇 TULIS KODE DI SINI:

    return result;
  }

  // 33. num1 > 0 || num2 < 5
  static bool? exercise33Solution(int num1, int num2) {
    bool? result;
    // 👇 TULIS KODE DI SINI:

    return result;
  }

  // 34. num1 > 0 && num2 < 5
  static bool? exercise34Solution(int num1, int num2) {
    bool? result;
    // 👇 TULIS KODE DI SINI:

    return result;
  }

  // 35. num1 < 0 || num2 > 5
  static bool? exercise35Solution(int num1, int num2) {
    bool? result;
    // 👇 TULIS KODE DI SINI:

    return result;
  }
}
