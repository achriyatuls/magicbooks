// LEMBAR KERJA SISWA - EFD1500 IF STATEMENT
//
// ════════════════════════════════════════════════════════════════════════════
// PETUNJUK PENGGUNAAN:
// ════════════════════════════════════════════════════════════════════════════
//
// 1. File ini BOLEH diubah untuk menjawab exercise
// 2. Tulis kode HANYA di area yang ditandai dengan "TULIS KODE DI SINI"
// 3. JANGAN mengubah:
//    - Nama function
//    - Parameter function
//    - Return statement (kecuali diminta)
//    - Variable yang sudah dideklarasikan (hanya boleh MENGISI nilainya)
//
// 4. Exercise dengan = VALIDATED (Multiple test cases)
// 5. Centang hijau untuk VALIDATED = SEMUA test cases passed
//
// ════════════════════════════════════════════════════════════════════════════

/// Class yang berisi jawaban siswa untuk semua exercise
class Efd1500Exercises {
  // ═══════════════════════════════════════════════════════════════════════════
  // EXERCISE 1: Cek Genap/Ganjil (VALIDATED)
  // ═══════════════════════════════════════════════════════════════════════════
  // INSTRUKSI:
  // - Cek apakah angka genap atau ganjil
  // - Return "Genap" jika genap, "Ganjil" jika ganjil
  //
  // HINT: Gunakan if-else dengan kondisi number % 2 == 0

  static String? exercise1Solution(int number) {
    String? result;

    // TULIS KODE DI SINI:

    return result; // ← JANGAN UBAH BARIS INI!
  }

  // ═══════════════════════════════════════════════════════════════════════════
  // EXERCISE 2: Cek Positif/Negatif/Nol (VALIDATED)
  // ═══════════════════════════════════════════════════════════════════════════
  // INSTRUKSI:
  // - Cek apakah angka positif, negatif, atau nol
  // - Return "Positif", "Negatif", atau "Nol"
  //
  // HINT: Gunakan if-else-if dengan kondisi num > 0, num < 0, atau num == 0

  static String? exercise2Solution(int num) {
    String? result;

    // TULIS KODE DI SINI:

    return result; // ← JANGAN UBAH BARIS INI!
  }

  // ═══════════════════════════════════════════════════════════════════════════
  // EXERCISE 3: Bandingkan Dua Angka (VALIDATED)
  // ═══════════════════════════════════════════════════════════════════════════
  // INSTRUKSI:
  // - Bandingkan num1 dan num2
  // - Return "Sama" jika sama, "num1 lebih besar" atau "num2 lebih besar"
  //
  // HINT: Gunakan if-else-if untuk membandingkan

  static String? exercise3Solution(int num1, int num2) {
    String? result;

    // TULIS KODE DI SINI:

    return result; // ← JANGAN UBAH BARIS INI!
  }

  // ═══════════════════════════════════════════════════════════════════════════
  // EXERCISE 4: Kategori Nilai (0-100) (VALIDATED)
  // ═══════════════════════════════════════════════════════════════════════════
  // INSTRUKSI:
  // - Tentukan kategori nilai berdasarkan score
  // - Return "A" (>= 90), "B" (>= 80), "C" (>= 70), "D" (>= 60), "E" (< 60)
  //
  // HINT: Gunakan if-else-if dengan kondisi dari tertinggi ke terendah

  static String? exercise4Solution(int score) {
    String? result;

    // TULIS KODE DI SINI:

    return result; // ← JANGAN UBAH BARIS INI!
  }

  // ═══════════════════════════════════════════════════════════════════════════
  // EXERCISE 5: Kategori Nilai (0-100) dengan Keterangan (VALIDATED)
  // ═══════════════════════════════════════════════════════════════════════════
  // INSTRUKSI:
  // - Tentukan kategori nilai dengan keterangan
  // - Return "Luar Biasa" (>= 90), "Baik" (>= 75), "Cukup" (>= 60), "Kurang" (< 60)
  //
  // HINT: Gunakan if-else-if

  static String? exercise5Solution(int score) {
    String? result;

    // TULIS KODE DI SINI:

    return result; // ← JANGAN UBAH BARIS INI!
  }

  // ═══════════════════════════════════════════════════════════════════════════
  // EXERCISE 6: Kategori Nilai dengan Grade (VALIDATED)
  // ═══════════════════════════════════════════════════════════════════════════
  // INSTRUKSI:
  // - Tentukan grade berdasarkan score
  // - Return "Grade A" (>= 85), "Grade B" (>= 70), "Grade C" (>= 55), "Grade D" (< 55)
  //
  // HINT: Gunakan if-else-if

  static String? exercise6Solution(int score) {
    String? result;

    // TULIS KODE DI SINI:

    return result; // ← JANGAN UBAH BARIS INI!
  }

  // ═══════════════════════════════════════════════════════════════════════════
  // EXERCISE 7: Cek Bilangan Bulat atau Tidak (VALIDATED)
  // ═══════════════════════════════════════════════════════════════════════════
  // INSTRUKSI:
  // - Cek apakah value adalah bilangan bulat (int) atau tidak
  // - Return "Bilangan Bulat" atau "Bukan Bilangan Bulat"
  //
  // HINT: Gunakan value is int

  static String? exercise7Solution(num value) {
    String? result;

    // TULIS KODE DI SINI:

    return result; // ← JANGAN UBAH BARIS INI!
  }

  // ═══════════════════════════════════════════════════════════════════════════
  // EXERCISE 8: Cek Negatif/Positif/Nol (VALIDATED)
  // ═══════════════════════════════════════════════════════════════════════════
  // INSTRUKSI:
  // - Cek apakah angka negatif, positif, atau nol
  // - Return "Negatif", "Positif", atau "Nol"
  //
  // HINT: Gunakan if-else-if

  static String? exercise8Solution(int number) {
    String? result;

    // TULIS KODE DI SINI:

    return result; // ← JANGAN UBAH BARIS INI!
  }

  // ═══════════════════════════════════════════════════════════════════════════
  // EXERCISE 9: Apakah number1 > number2 (VALIDATED)
  // ═══════════════════════════════════════════════════════════════════════════
  // INSTRUKSI:
  // - Cek apakah number1 lebih besar dari number2
  // - Return true atau false
  //
  // HINT: return number1 > number2

  static bool? exercise9Solution(int number1, int number2) {
    bool? result;

    // TULIS KODE DI SINI:

    return result; // ← JANGAN UBAH BARIS INI!
  }

  // ═══════════════════════════════════════════════════════════════════════════
  // EXERCISE 10: Apakah number1 < number2 (VALIDATED)
  // ═══════════════════════════════════════════════════════════════════════════
  // INSTRUKSI:
  // - Cek apakah number1 lebih kecil dari number2
  // - Return true atau false
  //
  // HINT: return number1 < number2

  static bool? exercise10Solution(int number1, int number2) {
    bool? result;

    // TULIS KODE DI SINI:

    return result; // ← JANGAN UBAH BARIS INI!
  }

  // ═══════════════════════════════════════════════════════════════════════════
  // EXERCISE 11: Apakah Hari Minggu (VALIDATED)
  // ═══════════════════════════════════════════════════════════════════════════
  // INSTRUKSI:
  // - Cek apakah hari ini adalah Minggu
  // - Return true jika Minggu (weekday == 7), false jika bukan
  //
  // HINT: Gunakan now.weekday == 7

  static bool? exercise11Solution(DateTime now) {
    bool? isSunday;

    // TULIS KODE DI SINI:

    return isSunday; // ← JANGAN UBAH BARIS INI!
  }

  // ═══════════════════════════════════════════════════════════════════════════
  // EXERCISE 12: Hitung Usia (Tahun) (VALIDATED)
  // ═══════════════════════════════════════════════════════════════════════════
  // INSTRUKSI:
  // - Hitung usia berdasarkan tanggal lahir dan hari ini
  // - Return usia dalam tahun
  //
  // HINT: age = today.year - birthday.year

  static int? exercise12Solution(DateTime birthday, DateTime today) {
    int? age;

    // TULIS KODE DI SINI:

    return age; // ← JANGAN UBAH BARIS INI!
  }

  // ═══════════════════════════════════════════════════════════════════════════
  // EXERCISE 13: Apakah Tanggal 1 Januari (VALIDATED)
  // ═══════════════════════════════════════════════════════════════════════════
  // INSTRUKSI:
  // - Cek apakah tanggal adalah 1 Januari
  // - Return true jika month == 1 dan day == 1
  //
  // HINT: date.month == 1 && date.day == 1

  static bool? exercise13Solution(DateTime date) {
    bool? isJanuaryFirst;

    // TULIS KODE DI SINI:

    return isJanuaryFirst; // ← JANGAN UBAH BARIS INI!
  }

  // ═══════════════════════════════════════════════════════════════════════════
  // EXERCISE 14: Apakah Hari Minggu (VALIDATED)
  // ═══════════════════════════════════════════════════════════════════════════
  // INSTRUKSI:
  // - Cek apakah hari adalah Minggu
  // - Return true jika weekday == 7
  //
  // HINT: date.weekday == 7

  static bool? exercise14Solution(DateTime date) {
    bool? isSunday;

    // TULIS KODE DI SINI:

    return isSunday; // ← JANGAN UBAH BARIS INI!
  }

  // ═══════════════════════════════════════════════════════════════════════════
  // EXERCISE 15: Apakah Hari Sabtu (VALIDATED)
  // ═══════════════════════════════════════════════════════════════════════════
  // INSTRUKSI:
  // - Cek apakah hari adalah Sabtu
  // - Return true jika weekday == 6
  //
  // HINT: date.weekday == 6

  static bool? exercise15Solution(DateTime date) {
    bool? isSaturday;

    // TULIS KODE DI SINI:

    return isSaturday; // ← JANGAN UBAH BARIS INI!
  }

  // ═══════════════════════════════════════════════════════════════════════════
  // EXERCISE 16: Apakah Hari Jumat (VALIDATED)
  // ═══════════════════════════════════════════════════════════════════════════
  // INSTRUKSI:
  // - Cek apakah hari adalah Jumat
  // - Return true jika weekday == 5
  //
  // HINT: date.weekday == 5

  static bool? exercise16Solution(DateTime date) {
    bool? isFriday;

    // TULIS KODE DI SINI:

    return isFriday; // ← JANGAN UBAH BARIS INI!
  }

  // ═══════════════════════════════════════════════════════════════════════════
  // EXERCISE 17: Apakah Hari Kamis (VALIDATED)
  // ═══════════════════════════════════════════════════════════════════════════
  // INSTRUKSI:
  // - Cek apakah hari adalah Kamis
  // - Return true jika weekday == 4
  //
  // HINT: date.weekday == 4

  static bool? exercise17Solution(DateTime date) {
    bool? isThursday;

    // TULIS KODE DI SINI:

    return isThursday; // ← JANGAN UBAH BARIS INI!
  }

  // ═══════════════════════════════════════════════════════════════════════════
  // EXERCISE 18: Apakah Hari Rabu (VALIDATED)
  // ═══════════════════════════════════════════════════════════════════════════
  // INSTRUKSI:
  // - Cek apakah hari adalah Rabu
  // - Return true jika weekday == 3
  //
  // HINT: date.weekday == 3

  static bool? exercise18Solution(DateTime date) {
    bool? isWednesday;

    // TULIS KODE DI SINI:

    return isWednesday; // ← JANGAN UBAH BARIS INI!
  }

  // ═══════════════════════════════════════════════════════════════════════════
  // EXERCISE 19: Apakah Hari Selasa (VALIDATED)
  // ═══════════════════════════════════════════════════════════════════════════
  // INSTRUKSI:
  // - Cek apakah hari adalah Selasa
  // - Return true jika weekday == 2
  //
  // HINT: date.weekday == 2

  static bool? exercise19Solution(DateTime date) {
    bool? isTuesday;

    // TULIS KODE DI SINI:

    return isTuesday; // ← JANGAN UBAH BARIS INI!
  }

  // ═══════════════════════════════════════════════════════════════════════════
  // EXERCISE 20: Apakah Hari Senin (VALIDATED)
  // ═══════════════════════════════════════════════════════════════════════════
  // INSTRUKSI:
  // - Cek apakah hari adalah Senin
  // - Return true jika weekday == 1
  //
  // HINT: date.weekday == 1

  static bool? exercise20Solution(DateTime date) {
    bool? isMonday;

    // TULIS KODE DI SINI:

    return isMonday; // ← JANGAN UBAH BARIS INI!
  }

  // ═══════════════════════════════════════════════════════════════════════════
  // EXERCISE 21: Apakah List Mengandung Angka 3 (VALIDATED)
  // ═══════════════════════════════════════════════════════════════════════════
  // INSTRUKSI:
  // - Cek apakah list mengandung angka 3
  // - Return true jika ada, false jika tidak
  //
  // HINT: Gunakan numbers.contains(3)

  static bool? exercise21Solution(List<int> numbers) {
    bool? hasThree;

    // TULIS KODE DI SINI:

    return hasThree; // ← JANGAN UBAH BARIS INI!
  }

  // ═══════════════════════════════════════════════════════════════════════════
  // EXERCISE 22: Apakah Semua Angka Genap (VALIDATED)
  // ═══════════════════════════════════════════════════════════════════════════
  // INSTRUKSI:
  // - Cek apakah semua angka dalam list adalah genap
  // - Return true jika semua genap, false jika ada yang ganjil
  //
  // HINT: Gunakan numbers.every((n) => n % 2 == 0)

  static bool? exercise22Solution(List<int> numbers) {
    bool? allEven;

    // TULIS KODE DI SINI:

    return allEven; // ← JANGAN UBAH BARIS INI!
  }

  // ═══════════════════════════════════════════════════════════════════════════
  // EXERCISE 23: Apakah Ada Angka > 5 (VALIDATED)
  // ═══════════════════════════════════════════════════════════════════════════
  // INSTRUKSI:
  // - Cek apakah ada angka lebih besar dari 5
  // - Return true jika ada, false jika tidak
  //
  // HINT: Gunakan numbers.any((n) => n > 5)

  static bool? exercise23Solution(List<int> numbers) {
    bool? hasGreaterThanFive;

    // TULIS KODE DI SINI:

    return hasGreaterThanFive; // ← JANGAN UBAH BARIS INI!
  }

  // ═══════════════════════════════════════════════════════════════════════════
  // EXERCISE 24: Apakah Ada Angka < 0 (VALIDATED)
  // ═══════════════════════════════════════════════════════════════════════════
  // INSTRUKSI:
  // - Cek apakah ada angka negatif (< 0)
  // - Return true jika ada, false jika tidak
  //
  // HINT: Gunakan numbers.any((n) => n < 0)

  static bool? exercise24Solution(List<int> numbers) {
    bool? hasLessThanZero;

    // TULIS KODE DI SINI:

    return hasLessThanZero; // ← JANGAN UBAH BARIS INI!
  }

  // ═══════════════════════════════════════════════════════════════════════════
  // EXERCISE 25: Apakah Jumlah Ganjil == Jumlah Genap (VALIDATED)
  // ═══════════════════════════════════════════════════════════════════════════
  // INSTRUKSI:
  // - Cek apakah jumlah angka ganjil sama dengan jumlah angka genap
  // - Return true jika sama, false jika tidak
  //
  // HINT: Hitung jumlah ganjil dan genap dengan where, lalu bandingkan

  static bool? exercise25Solution(List<int> numbers) {
    bool? oddCountEqualEvenCount;

    // TULIS KODE DI SINI:

    return oddCountEqualEvenCount; // ← JANGAN UBAH BARIS INI!
  }

  // ═══════════════════════════════════════════════════════════════════════════
  // EXERCISE 26: Apakah Nilai Terbesar == 5 (VALIDATED)
  // ═══════════════════════════════════════════════════════════════════════════
  // INSTRUKSI:
  // - Cek apakah nilai terbesar dalam list adalah 5
  // - Return true jika ya, false jika tidak
  //
  // HINT: Gunakan numbers.reduce((a, b) => a > b ? a : b) == 5

  static bool? exercise26Solution(List<int> numbers) {
    bool? largestIsFive;

    // TULIS KODE DI SINI:

    return largestIsFive; // ← JANGAN UBAH BARIS INI!
  }

  // ═══════════════════════════════════════════════════════════════════════════
  // EXERCISE 27: Apakah Nilai Terkecil == 1 (VALIDATED)
  // ═══════════════════════════════════════════════════════════════════════════
  // INSTRUKSI:
  // - Cek apakah nilai terkecil dalam list adalah 1
  // - Return true jika ya, false jika tidak
  //
  // HINT: Gunakan numbers.reduce((a, b) => a < b ? a : b) == 1

  static bool? exercise27Solution(List<int> numbers) {
    bool? smallestIsOne;

    // TULIS KODE DI SINI:

    return smallestIsOne; // ← JANGAN UBAH BARIS INI!
  }

  // ═══════════════════════════════════════════════════════════════════════════
  // EXERCISE 28: Apakah Jumlah Item == 5 (VALIDATED)
  // ═══════════════════════════════════════════════════════════════════════════
  // INSTRUKSI:
  // - Cek apakah jumlah item dalam list adalah 5
  // - Return true jika ya, false jika tidak
  //
  // HINT: Gunakan numbers.length == 5

  static bool? exercise28Solution(List<int> numbers) {
    bool? countIsFive;

    // TULIS KODE DI SINI:

    return countIsFive; // ← JANGAN UBAH BARIS INI!
  }

  // ═══════════════════════════════════════════════════════════════════════════
  // EXERCISE 29: Apakah Ada Duplikat (VALIDATED)
  // ═══════════════════════════════════════════════════════════════════════════
  // INSTRUKSI:
  // - Cek apakah ada angka yang duplikat dalam list
  // - Return true jika ada duplikat, false jika tidak
  //
  // HINT: Bandingkan numbers.length dengan numbers.toSet().length

  static bool? exercise29Solution(List<int> numbers) {
    bool? hasDuplicate;

    // TULIS KODE DI SINI:

    return hasDuplicate; // ← JANGAN UBAH BARIS INI!
  }

  // ═══════════════════════════════════════════════════════════════════════════
  // EXERCISE 30: Cek Negatif/Positif/Nol (VALIDATED)
  // ═══════════════════════════════════════════════════════════════════════════
  // INSTRUKSI:
  // - Cek apakah angka negatif, positif, atau nol
  // - Return "Negatif", "Positif", atau "Nol"
  //
  // HINT: Gunakan if-else-if

  static String? exercise30Solution(int number) {
    String? result;

    // TULIS KODE DI SINI:

    return result; // ← JANGAN UBAH BARIS INI!
  }

  // ═══════════════════════════════════════════════════════════════════════════
  // EXERCISE 31: num1 > 0 && num2 > 5 (VALIDATED)
  // ═══════════════════════════════════════════════════════════════════════════
  // INSTRUKSI:
  // - Cek apakah num1 > 0 DAN num2 > 5
  // - Return true atau false
  //
  // HINT: return num1 > 0 && num2 > 5

  static bool? exercise31Solution(int num1, int num2) {
    bool? result;

    // TULIS KODE DI SINI:

    return result; // ← JANGAN UBAH BARIS INI!
  }

  // ═══════════════════════════════════════════════════════════════════════════
  // EXERCISE 32: num1 > 0 || num2 > 15 (VALIDATED)
  // ═══════════════════════════════════════════════════════════════════════════
  // INSTRUKSI:
  // - Cek apakah num1 > 0 ATAU num2 > 15
  // - Return true atau false
  //
  // HINT: return num1 > 0 || num2 > 15

  static bool? exercise32Solution(int num1, int num2) {
    bool? result;

    // TULIS KODE DI SINI:

    return result; // ← JANGAN UBAH BARIS INI!
  }

  // ═══════════════════════════════════════════════════════════════════════════
  // EXERCISE 33: num1 > 0 || num2 < 5 (VALIDATED)
  // ═══════════════════════════════════════════════════════════════════════════
  // INSTRUKSI:
  // - Cek apakah num1 > 0 ATAU num2 < 5
  // - Return true atau false
  //
  // HINT: return num1 > 0 || num2 < 5

  static bool? exercise33Solution(int num1, int num2) {
    bool? result;

    // TULIS KODE DI SINI:

    return result; // ← JANGAN UBAH BARIS INI!
  }

  // ═══════════════════════════════════════════════════════════════════════════
  // EXERCISE 34: num1 > 0 && num2 < 5 (VALIDATED)
  // ═══════════════════════════════════════════════════════════════════════════
  // INSTRUKSI:
  // - Cek apakah num1 > 0 DAN num2 < 5
  // - Return true atau false
  //
  // HINT: return num1 > 0 && num2 < 5

  static bool? exercise34Solution(int num1, int num2) {
    bool? result;

    // TULIS KODE DI SINI:

    return result; // ← JANGAN UBAH BARIS INI!
  }

  // ═══════════════════════════════════════════════════════════════════════════
  // EXERCISE 35: num1 < 0 || num2 > 5 (VALIDATED)
  // ═══════════════════════════════════════════════════════════════════════════
  // INSTRUKSI:
  // - Cek apakah num1 < 0 ATAU num2 > 5
  // - Return true atau false
  //
  // HINT: return num1 < 0 || num2 > 5

  static bool? exercise35Solution(int num1, int num2) {
    bool? result;

    // TULIS KODE DI SINI:

    return result; // ← JANGAN UBAH BARIS INI!
  }
}
